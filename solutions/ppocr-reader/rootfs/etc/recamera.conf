# reCamera Global Configuration
# Shared by all reCamera solutions (ppocr-reader, face-analysis, yolo-detector, etc.)
# Each solution can override these in its own /etc/<solution>.conf

# MQTT Broker
MQTT_HOST=localhost
MQTT_PORT=1883
